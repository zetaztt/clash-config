# AGENTS.md

## 项目概览

本仓库是一个使用 TypeScript、`tsx` 和 npm 的个人配置生成器，用于按需生成 Clash Verge Rev / Mihomo 配置。住宅代理链路是当前支持的需求之一，不是仓库唯一或固定的用途。可以纳入版本控制的内容包括公开的类型化配置、TypeScript 源码和使用内联合成值的测试；真实订阅地址、节点凭据、Provider 缓存及生成配置均属于机密信息。

现有住宅代理链路必须保持以下路由方向；新增其他配置需求时，不应默认套用住宅代理模型：

```text
普通流量：客户端 -> 上游代理节点 -> 目标
住宅流量：客户端 -> 上游代理节点 -> 配置的住宅节点 -> 目标
```

- 用户可见组名与当前组结构以 `MihomoProxyNodeGroup`、`MihomoProxyPolicyGroup` 和 `createMihomoConfig` 为准，不在本文件重复维护名称清单或数量。节点组与策略组必须使用不同枚举，`rulesConfigs` 只能引用策略组或允许的 Mihomo 内置策略，不能直接引用节点组。`AdPolicy` 规则块必须是 `rulesConfigs` 的第一个活动规则块：选择 `REJECT` 时优先拦截广告流量，选择 `PASS` 时跳过当前广告规则并继续后续匹配。新增其他代理组必须有独立、明确的配置需求。
- 每个住宅节点都必须通过 `dialer-proxy` 指向 `ProxyNodes` 建立链式代理，不能直接连接目标；节点名称不作固定要求。
- 住宅规则必须位于使用 `FinalPolicy` 的最终 `MATCH` 之前；该 `MATCH` 必须是最后一条活动规则。
- `rulesConfigs` 的外层规则块按声明顺序确定优先级；除广告规则块必须位于第一位等明确不变量外，当前顺序可随明确的路由需求调整，不得在测试中快照或写死完整外层顺序。存在策略优先级关系时必须拆成不同规则块，没有优先级差异的同用途、同出口规则按类型聚合在同一 `rules` 对象中。生成器不得按代理组或规则类型自动重排，并始终在末尾追加 `MATCH`。
- 现有路由有意不包含 UDP/443 阻断规则，以保留 QUIC / HTTP/3。只有用户明确提出阻断需求时才能改变此行为，并同步更新测试和 README。
- 内联规则不得加入无法等价表达的独立 `port`、`network` 和 `protocol` 条件。`GEOIP,private,DIRECT` 是目标 IP 规则的解析边界：位于其之前的目标 IP 规则必须带 `no-resolve`，从该规则开始及其后的目标 IP 规则允许解析；域名规则不适用 `no-resolve`。不同显式规则块包含相同 matcher 时，继续按 `rulesConfigs` 的声明顺序匹配，不做跨规则块去重或冲突拒绝。

## 安全边界

- 禁止读取、打印、总结、比较、暂存、提交或分享任何匹配 `proxies*.json` 的文件，以及 `dist/` 和 `proxy_providers/` 中的内容。
- 生成的 YAML 即使已被 Git 忽略也必须视为机密，因为其中包含订阅令牌和 SOCKS5 凭据。
- 排查用户配置问题时，只接受使用合成占位值制作的脱敏副本，不索取或处理真实凭据。
- 新增任何机密输入、缓存或生成输出路径时，必须先同时加入 `.gitignore` 和 `.aiignore`。
- 除非用户明确要求且已经说明安全影响，否则不得削弱或删除任何忽略规则。
- 错误消息、日志、测试快照和命令输出不得包含私有配置值或生成配置内容。

## 代码职责

- `scripts/config.ts` 保存公开、类型化的 Mihomo 配置和所有内联路由规则，包括当前的住宅代理链路；私有值只能通过 `ProxiesConfig` 参数注入。
- `scripts/mihomo-types.ts` 保存本项目实际使用的 Mihomo 原生配置子集；`scripts/config-types.ts` 保存项目自定义的代理输入、构建选项、代理组和规则配置类型。
- `scripts/build.ts` 负责命令行参数、私有设置校验、YAML 序列化和输出覆盖。
- `test/` 使用 Node.js test runner 和 `tsx`，以合成配置验证固定配置结构与路由不变量，并只在系统临时目录中生成输出。
- `README.md` 负责面向用户的安装、设置、生成和导入说明；AGENTS.md 只记录 Agent 不易可靠推断的约束与流程。

## 开发流程

1. 修改前先沿相关调用链核对类型、配置创建、输入校验、序列化、输出替换和测试，不依据旧文档猜测当前实现。
2. 以完成当前任务所需的最小改动为边界，沿用现有命名、职责划分和命令行行为，不夹带无关重构。
3. 发现任务范围外的问题时，先完成并验证当前任务，再在交付说明中提出可独立评审的建议；只有问题直接阻塞正确实现时，才进行影响范围最小的必要重构并说明原因。
4. 修改代理设置字段时，同步更新 `ProxiesConfig`、输入校验、配置生成、测试中的合成值和 README。
5. 修改路由、代理节点或代理组时，同时检查链式代理方向、代理组数量、规则顺序及相应的负向测试。
6. 修改 CLI 路径或文件写入逻辑时，继续显式解析路径、以 UTF-8 写入，并在输入通过校验后直接覆盖目标文件。
7. 根据改动范围执行验证，并仅检查本次涉及的公开文件差异；命令生成的文件只有属于当前任务且不含机密时才可保留。

## 编码规范

- 支持 Node.js 24.3 及以上版本，保持 ECMAScript modules、严格 TypeScript 和当前 NodeNext 解析方式。
- TypeScript 通过 `tsx` 直接运行，不生成或提交编译后的 JavaScript。
- 保持公开配置为类型化对象，并使用 `yaml` 包序列化；不要手写或拼接完整 YAML。
- 在写入输出前校验所有私有输入的必填值、端口范围和 URL 协议。输入校验失败不得留下新输出或临时文件；固定路由结构由测试保障。
- 优先使用 Node.js 内置模块；只有依赖能显著简化或提高正确性时才引入新的运行时依赖。
- 错误信息应指出无效字段或结构，但不得回显订阅 URL、主机、用户名、密码或其他私有值。
- 注释说明意图、不变量和非显而易见的风险，不复述名称或类型。无法从名称和类型直接判断职责的导出定义优先使用多行 JSDoc。
- 涉及凭据边界、输出覆盖、路由顺序或链式代理方向时，在关键定义或实现处保留设计原因；不要为任务无关的旧代码批量补注释。
- 依赖变更使用根目录 `package-lock.json`，同步提交清单与 lockfile，不引入第二套包管理器。

## 测试与验证

验证按改动范围和风险由窄到宽执行：

- 仅修改文档或注释：检查目标文件和公开差异，无需运行 TypeScript 构建。
- 修改 TypeScript、类型或输入校验逻辑：运行 `npm run typecheck` 和 `npm test`。
- 修改设置字段、生成器、序列化、文件替换、路由或配置结构：除类型检查和测试外，在系统临时目录创建合成代理设置并生成配置：

    ```text
    npm exec tsx -- scripts/build.ts --proxies <系统临时目录中的合成 JSON> --output <系统临时目录中的路径>
    ```

所有自动验证只能使用测试内构造或系统临时目录中的合成值，生成输出必须放在系统临时目录并在验证后删除。不得使用或验证真实的 `proxies.json`。

验证结束后检查 `git status --short`，并仅对本次涉及的公开文件检查差异，确认没有私有或生成文件进入变更。TypeScript 改动还需审查本次新增或实质修改定义的注释是否充分，并在交付说明中报告验证结果。

## Git 与变更管理

- 保持改动单一目的，保留用户已有的无关修改，不覆盖或整理任务范围外的文件。
- 任何情况下都不得提交、推送、发布或分享生成配置及其他机密文件；对公开源码执行提交或推送也必须由用户明确要求。
- Git 操作和差异检查只面向已确认安全的公开路径，不对被忽略的私有或生成内容执行强制添加、全文搜索或差异比较。
- 提交说明如由用户要求创建，应简短描述核心变更且不包含任何私有值。

## AGENTS.md 维护规则

- 本文件只记录 Agent 难以从代码和配置可靠推断、违反后影响较大的项目规则，不复制通用编程知识、Git 基础知识或 README 内容。
- 优先记录长期有效的安全边界、行为不变量、职责划分和必要流程；可由代码扫描得到的文件清单或短期实现细节不应持续扩张。
- 新增规则前确认其适用范围、违反风险和必要流程；已有工具能够自动保证的要求无需重复记录。
- 修改本文件前核对当前目录、命令、配置和实现。代码与本文件冲突时，先确认真实意图，不延续已不存在的结构或命令。
- 保持内容简洁且面向后续任务；定期删除过时、重复或已由自动化约束覆盖的规则。
